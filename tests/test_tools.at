m4_include([package.m4])
m4_include([test_macros.at])

include_test_inputs([cregexport])
include_test_inputs([creginfo])

AT_TEST_HELPER_FN(
  [run_cregtools_binary],
  [path arguments],
  [Resolves and runs a cregtools binary],
  [if test -x "$abs_top_builddir/cregtools/$[]1"; then
    BINARY="$abs_top_builddir/cregtools/$[]1"
  elif test -x "$abs_top_builddir/cregtools/$[]1.exe"; then
    # Prevent wine debug messages failing the test.
    WINEDEBUG=-all

    BINARY="$abs_top_builddir/cregtools/$[]1.exe"
  else
    echo "Missing binary: $[]1"
    exit 127
  fi
  shift

  $BINARY $[]*
  ])

AT_TEST_HELPER_FN(
  [run_cregtools_binary_with_reference_output],
  [tool_name arguments test_file],
  [Runs a cregtools binary and compares its output against a reference],
  [CREGTOOL="$[]1"

  shift
  for TEST_FILE do :; done
   
  FILENAME=`basename "${TEST_FILE}"`
  TEST_SET=`dirname "${TEST_FILE}"`
  TEST_SET=`basename "${TEST_SET}"`
  OUTPUT_FILE="${FILENAME}.log"

  ath_fn_run_cregtools_binary "${CREGTOOL}" $[]* > "${OUTPUT_FILE}"
  STATUS=$?

  if test ${STATUS} -eq 0; then
    # Strip header with version.
    ${SED} -i'~' '1,2d' "${OUTPUT_FILE}"

    EXPECTED_RESULTS="$abs_srcdir/input/.${CREGTOOL}/${TEST_SET}/${FILENAME}.gz"
    if test -f "${EXPECTED_RESULTS}"; then
      # zcat on Mac OS requires the input from stdin.
      ${ZCAT} < "${EXPECTED_RESULTS}" | diff "${OUTPUT_FILE}" -;
      STATUS=$?
    else
      ${GZIP_COMMAND} "${OUTPUT_FILE}"
      mkdir -p "${EXPECTED_RESULTS}"
      mv "${OUTPUT_FILE}.gz" "${EXPECTED_RESULTS}"
    fi
  fi
  exit $STATUS
  ])

dnl Runs a cregtool and compares its output against a reference
dnl Arguments:
dnl   tool_name
dnl   test_inputs
m4_define([RUN_TEST_CREGTOOL],
  [m4_foreach_w(
    [test_input],
    [$2],
    [m4_define([option_set], m4_normalize(get_test_result_value([test_input], 1)))
    m4_define([test_options], m4_normalize(get_test_result_value([test_input], 2)))
    m4_define([test_file], m4_normalize(get_test_result_value([test_input], 3)))

    m4_define([sanitized_test_file],
      [m4_translit([test_file], [[],[],[]"'`\], [______])])

    m4_ifblank(
      test_options,
      [AT_SETUP($1 with input: 'sanitized_test_file')

      AT_SKIP_IF(
        [test -n "${SKIP_TOOLS_TESTS}" || test -n "${SKIP_TOOLS_END_TO_END_TESTS}"])

      AT_SKIP_IF([test -z "${GZIP_COMMAND}"])
      AT_SKIP_IF([test -z "${SED}"])
      AT_SKIP_IF([test -z "${ZCAT}"])

      TEST_FILE=`ath_fn_resolve_test_file "$abs_srcdir/input/test_file"`

      AT_CHECK(ath_fn_run_cregtools_binary_with_reference_output $1 "${TEST_FILE}")

      AT_CLEANUP],
      [AT_SETUP($1 with options: 'test_options' and input: 'sanitized_test_file')

      AT_SKIP_IF(
        [test -n "${SKIP_TOOLS_TESTS}" || test -n "${SKIP_TOOLS_END_TO_END_TESTS}"])

      AT_SKIP_IF([test -z "${GZIP_COMMAND}"])
      AT_SKIP_IF([test -z "${SED}"])
      AT_SKIP_IF([test -z "${ZCAT}"])

      TEST_FILE=`ath_fn_resolve_test_file "$abs_srcdir/input/test_file"`

      AT_CHECK(ath_fn_run_cregtools_binary_with_reference_output $1 test_options "${TEST_FILE}")

      AT_CLEANUP])]
    )]
  )]
)

dnl Wrap m4_define to overwrite AT_TESTSUITE_NAME
m4_define([_M4_DEFINE_HOOK], m4_defn([m4_define]))
m4_define([m4_define],
  [m4_if(
    [$1],
    [AT_TESTSUITE_NAME],
    [_M4_DEFINE_HOOK([$1], [Running tools tests])],
    [_M4_DEFINE_HOOK([$1], [$2])])])

dnl Redefine AS_BOX to remove the "box"
m4_undefine([AS_BOX])
m4_define([AS_BOX], [echo "$1"])

AT_INIT([tools])
AT_COLOR_TESTS

dnl Rewrite AT_SETUP to change the minimum line length
m4_define([AT_SETUP],
  m4_unquote(
    [m4_bpatsubst(
      m4_dquote(m4_defn([AT_SETUP])),
      [47],
      [72])
    ])
  )

SETUP_HELPER_FUNCTIONS

RUN_TEST_BINARIES(
  [SKIP_TOOLS_TESTS],
  [tools_info_handle tools_mount_path_string tools_output tools_signal])

RUN_TEST_CREGTOOL(
  [creginfo],
  test_inputs_creginfo)

RUN_TEST_CREGTOOL(
  [cregexport],
  test_inputs_cregexport)
